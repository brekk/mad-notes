import IO from "IO"
import List from "List"
import String from "String"



say :: String -> String -> String
say = (verb, subject) => verb ++ " " ++ subject

main = () => {
  pipe(
    say("hello"),
    IO.putLine,
  )("world")
}
